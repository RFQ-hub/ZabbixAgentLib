branches:
  only:
    - master
image:
  - Visual Studio 2017
version: '{build}'
cache:
  - "%LocalAppData%\\NuGet\\Cache -> paket.lock"
  - "paket-files -> paket.lock"
build_script:
  - dir
  - .\build.cmd CI
test: off
